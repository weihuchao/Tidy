## MacOS

```bash
find . -type f -name '._*'
find . -type f -name '._*' -print0
find . -type f -name '._*' -delete

find . -type f -name '.DS_Store'
find . -type f -name '.DS_Store' -print0
find . -type f -name '.DS_Store' -delete
```

## 打包

```bash
for d in */; do 7z a -mx0 "${d%/}.7z" "./$d*" && rm -rf "$d"; done
for d in */; do 7z a -mx0 -p998 "${d%/}.7z" "./$d*" && rm -rf "$d"; done

f='name';7z a -mx0 "${f}.7z" ./"${f}"/* && rm -rf ./"${f}"

f='4';7z a -mx0 -p998 "${f}.7z" ./"${f}"/* && rm -rf ./"${f}"
```

## 

```bash
ffmpeg -i "1.mp4" -i "1.m4a" -c:v copy -c:a aac -strict experimental -map 0:v:0 -map 1:a:0 "中式洗腦謊言.mp4"

```

## 

```bash
chflags -R nouchg /Volumes/MSData/Pictures
```

##

```bash
dot_clean -m ./1
xattr -rc ./1

find ./1 -name '._*' -print
find ./1 -name '._*' -delete


dot_clean -m .
xattr -rc .
for d in */; do 7z a -mx0 -p998 "${d%/}.7z" "./$d*" && rm -rf "$d"; done
```